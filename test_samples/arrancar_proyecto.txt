# 1. Abre Docker Desktop (icono barra de tareas)

# 2. Activa el venv
ent_email\Scripts\activate

# 3. Terminal A → Django
python manage.py runserver

# 4. Terminal B → Cloudflare Tunnel (reemplaza a ngrok)

cloudflared tunnel run dockershield

# 5. Listo — abre https://app.dockershield.lat
#    La URL ya está configurada en SendGrid Inbound Parse,
#    los correos llegan solos al webhook.


#    Opción B (respaldo) → ngrok con tu dominio fijo
#    Si usás esta opción, hay que cambiar también la URL en SendGrid
#    Inbound Parse a:  https://twilight-baking-viewing.ngrok-free.dev/webhook/inbound/
#    y actualizar SITE_URL + ALLOWED_HOSTS en .env
ngrok http --url=twilight-baking-viewing.ngrok-free.dev 8000



#Si hago cambios en reglas yara, rebuildear la imagen docker:
docker build -t email_seguro_sandbox -f Dockerfile.sandbox .
