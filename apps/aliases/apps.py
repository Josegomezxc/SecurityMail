from django.apps import AppConfig


class AliasesConfig(AppConfig):
    name = 'apps.aliases'
    label = 'aliases'
    default_auto_field = 'django.db.models.BigAutoField'
    verbose_name = 'Alias desechables'
